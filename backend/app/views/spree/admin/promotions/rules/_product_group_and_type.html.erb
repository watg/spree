<div class="field alpha omega eight columns">
  <%= label_tag "#{param_prefix}_product_group_ids_string", Spree.t('product_group_rule.choose_product_groups') %>
  <%= hidden_field_tag "#{param_prefix}[product_group_ids_string]", promotion_rule.product_group_ids.join(","), :class => "product_group_picker fullwidth" %>
  <br><br>
</div>
<br><br><br>
<div class="field alpha omega eight columns">
  <%= label_tag "#{param_prefix}_product_type_ids", Spree.t('product_rule.choose_product_types') %>
  
  <%= select_tag "#{param_prefix}[product_type_ids]", options_for_select(Spree::MartinProductType.all.collect {|x| [x.name, x.id]}, promotion_rule.product_type_ids), :multiple => true, class: 'select2 fullwidth', id: "promotion_rule_product_type" %>
</div>
