<% if @outcome.valid? %>
  <% snippet = nil %>
  <% ActionView::Helpers::FormBuilder.new(:assembly_definition, @assembly_definition, self, {}).fields_for(:images, :child_index => @outcome.result.image.id) do |image_form| %>
    <% snippet = capture do %>
      <%= render 'spree/admin/assembly_definition_images/image', :f => image_form %>
    <% end %>
  <% end %>

  $('#upload_<%=params[:unique_id]%>').hide();
  $('.no-objects-found').hide();
  $('#images_table_body').append('<%= j snippet %>');
  activateSelect2();
<% else %>
  show_flash('error', '<%= @outcome.errors.full_messages.to_sentence %>');
<% end %>
