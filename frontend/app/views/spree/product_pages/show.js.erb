$('.loading-text').remove();
$('.variants').append('<%= j render partial: 'variant', :layout => false, :collection => @variants, :as => :variant %>');
$('.pagination').replaceWith('<%= j paginate @variants %>');
