$('.loading-text').remove();
$('.variants').append('<%= j render partial: 'variant', :layout => false, :collection => @variants, :as => :variant, :locals => @product_page.tab_made_by_the_gang %>');
$('.pagination').replaceWith('<%= j paginate @variants %>');
