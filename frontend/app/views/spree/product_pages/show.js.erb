$('.loading-text').remove();
$('.variants').append('<%= j render partial: 'variant', :layout => false, :collection => @variants, :as => :variant, :locals => {tab: @product_page.made_by_the_gang} %>');
$('.pagination').replaceWith('<%= j paginate @variants %>');
