<!DOCTYPE html>
<!--[if IE 8]><html class="no-js lt-ie9" lang="en" ><![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js" lang="en" > <!--<![endif]-->
<head data-hook="inside_head">
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="p:domain_verify" content="d55a740b763bcb14c5af217ac8eb2b21" />
  <title><%= content_for?(:title) ? yield(:title) : title || "Wool And The Gang" %></title>
  <%= content_for?(:meta) ? yield(:meta) : meta_data_tags.html_safe %>
  <script type="text/javascript" src="https://photorankstatics-a.akamaihd.net/static/frontend/latest/build.min.js" async></script>

  <%= stylesheet_link_tag    "application", cache: true %>
  <%= javascript_include_tag "application"%>

  <script type="text/javascript" src="https://s3.amazonaws.com/mamaya/mamaya_spree_1276.js" defer="defer"></script>
  <%= yield(:javascript_include) %>

  <%= csrf_meta_tags %>
  <%= render "spree/shared/frontend_routes" %>
  <%= yield :head %>
</head>

<body id="<%= @body_id || 'default' %>" class="shop <%= body_class %> <%= device_class %>" data-hook="body">

  <%= render :partial => 'spree/shared/header' %>

  <% if Flip.on?(:suites_feature) %>
  <%= render "spree/shared/taxons_driven_menu" %>
  <% else %>
  <%= render "spree/shared/old_menu" %>
  <% end %>

  <%= render "spree/shared/shipping_and_contact" %>

  <div class="container">
    <div class="row">
      <div class="large-12 columns">
        <%= flash_messages %>
        <%= yield %>
      </div>
    </div>

    <%= render "spree/shared/footer" %>
  </div>

  <script>
    var setApiKey = function(e) {
        Spree.api_key = <%= raw(try_spree_current_user.try(:spree_api_key).to_s.inspect) %>;
    };
    document.addEventListener("DOMContentLoaded", setApiKey);
    document.addEventListener("page:load", setApiKey);
  </script>

  <%= render :partial => 'spree/shared/facebook_pixel' %>
  <%#= render :partial => 'spree/shared/adroll_pixel' %>
  <%= render :partial => 'spree/shared/google_universal_analytics' %>

</body>
</html>
